# netsec-chat
A secure relay-based chat system.

First, setup the keys by running `python3 keygen.py`.

Afterwards, you can run the program using `python3 relay.py` and `python3 client.py` in separate windows.
